name: Release

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  windows:
    strategy:
      matrix:
        arch: ["x86-64-bmi2", "x86-64-modern", "x86-64"]
    runs-on: windows-2022

    steps:
      - uses: al-cheb/configure-pagefile-action@v1.2	
        with:	
            minimum-size: 16GB
            maximum-size: 16GB
            disk-root: "C:"

      - uses: actions/checkout@v3

      - name: make
        run: cd src && make clean && make -j build COMP=mingw ARCH=${{ matrix.arch }} EXE=fairy-sanmill_${{ matrix.arch }}.exe && strip fairy-sanmill_${{ matrix.arch }}.exe

      - name: make largeboards
        run: cd src && make clean && make -j build COMP=mingw ARCH=${{ matrix.arch }} EXE=fairy-sanmill-largeboards_${{ matrix.arch }}.exe largeboards=yes && strip fairy-sanmill-largeboards_${{ matrix.arch }}.exe

      - name: make all
        if: ${{ matrix.arch == 'x86-64' }}
        run: cd src && make clean && make -j build COMP=mingw ARCH=${{ matrix.arch }} EXE=fairy-sanmill-all_${{ matrix.arch }}.exe largeboards=yes all=yes && strip fairy-sanmill-all_${{ matrix.arch }}.exe

      - uses: actions/upload-artifact@v3
        with:
          name: fairy-sanmill
          path: src/fairy-sanmill*.exe

  linux:
    strategy:
      matrix:
        arch: ["x86-64-bmi2", "x86-64-modern", "x86-64"]
    runs-on: ubuntu-20.04

    steps:
      - uses: actions/checkout@v3

      - name: make
        run: cd src && make clean && make -j build COMP=gcc ARCH=${{ matrix.arch }} EXE=fairy-sanmill_${{ matrix.arch }} && strip fairy-sanmill_${{ matrix.arch }}

      - name: make largeboards
        run: cd src && make clean && make -j build COMP=gcc ARCH=${{ matrix.arch }} EXE=fairy-sanmill-largeboards_${{ matrix.arch }} largeboards=yes && strip fairy-sanmill-largeboards_${{ matrix.arch }}

      - name: make all
        if: ${{ matrix.arch == 'x86-64' }}
        run: cd src && make clean && make -j build COMP=gcc ARCH=${{ matrix.arch }} EXE=fairy-sanmill-all_${{ matrix.arch }} largeboards=yes all=yes && strip fairy-sanmill-all_${{ matrix.arch }}

      - uses: actions/upload-artifact@v3
        with:
          name: fairy-sanmill
          path: src/fairy-sanmill*
